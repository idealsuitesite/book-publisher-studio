# Version Index

A running journal of every tagged version and where the product is headed. For exact sprint scope, see `docs/TODO.md` and the per-sprint Level 2 Design Reviews in `docs/architecture/diagrams/` — this file tracks the version-to-milestone mapping, not day-to-day task detail. (**Pointer corrected 2026-07-18, ADR-0040 Correction 3:** this line previously named `docs/ROADMAP.md` as authoritative for sprint scope; that document has been annotated as superseded, being roughly eight sprints stale.)

**State note (2026-07-21):** the work of Sprints 9-11 — UI Foundation grown into the "L'Atelier" studio (PRODUCT_EXPERIENCE/VISUAL_LANGUAGE), Home/Workspace (absorbing much of Sprint 10's planned UX scope), and SQLite persistence (ADR-0048) — plus the CTO-ordered **fidelity detour** (Import Fidelity ADR-0049, Render Drift ADR-0051, doctrine ADR-0050) is **merged to `main`** via PR #14 and PR #15, fully verified (backend 556/556, real-corpus harnesses green, visual baseline byte-identical), and **deliberately untagged**: the next version/tag decision is the CTO's, likely after the `PUBLICATION_QUALITY_BAR.md` calibration review. The v0.10-v0.12 rows below keep their planned shape for the record; their *content* largely exists on `main` today. **Further untagged merges since (2026-07-22 post-merge, `main` @ `aa5ac9b` + docs):** verify-publication-quality (DOCX §4), the second theme "Modern", the `AIProvider` anchor port, **manual structure editing (COMPLETE — organize + create, through `fb76960`)**, then a run of measured mini-Level-2 chantiers — **subtitle spacing** (`d3c0f53`, + a latent ADR-0051 empty-title drift closed), **editorial parts** (reporting `1806809` + export placement `397bc2c` — the studio and the exported book now agree on front/back matter), **per-project accent override** (`f74db29`, R2-free), **Living-Proof first-open nudge** (`20ca8ad`), **section folding** in the Structure station (`1a6cf8a`, a regression restored), a **conditional first-screen layout** (`0093bb5`), then — **Performance (S13), OPENED and CLOSED on three measured verdicts** — **accent-only pagination reuse** (`bfc756f`, `MINI_DR_PAGINATION_REUSE`: a `PaginationCache` port + LRU, keyed without accent, re-inked with the fresh typeset; Option 1 font-cache and Option 3 render micro-opt both closed on evidence — ~2ms and a ~10ms/<3% safe ceiling respectively; S13 reopens only on a real user latency signal), and — **the "Part" level BUILT** (`1e3df5b`, `PART_LEVEL_STRUCTURE`, Shape B: flat `Chapter.partOpener` dividers, positional grouping, continuous numbering openers-excluded; its prerequisite commit closed a real latent ADR-0051 drift the scope spike measured — a titled blockless chapter was invisible to the model while every renderer drew it; verified live on faith-alone, PDF 91→90 pages on divider removal, exactly +1). And — **the Home tightened** (`a60eb02`, `HOME_TIGHTEN_SCOPE` A+B1: promise-first wording on the empty screen with the pitch's absence on return pinned by test; a hero Continue card for the last-worked-on project over the compact grid; frontend-only, verified live incl. the hero flipping after an edit on the oldest project; the dev fixture library cleaned, a 3-real-manuscript demo library kept). Then the **CTO queue directive** (2026-07-21): item 1 **KDP margin/gutter validation** merged (`MarginComplianceRule` — the dead gutter table's first consumer, accidental → enforced compliance); item 3 **front-matter editing** merged (`1c6af56`, Phase 3b closed — edit/clear the rendered title & copyright pages, the Q3 vanish author-triggered); item 4 **typography tuning** merged (`5bc8f65` — text-size presets + font pairing through the one `resolveTheme` seam, the pagination-cache key extended); item 2 **BLOCKED** (2-of-3 corpus fixtures, as instructed to say); item 5 **drop caps DELIVERED AND MERGED** (`aa5ac9b`, 4 commits — `Theme.presentation.dropCap`, tri-format native drop caps from one declared value and one derived arithmetic incl. Word's real framePr, the `DROPCAP_PARAGRAPH_ATOMICITY` owed test delivered, three instrument-liar records in `MINI_DR_DROP_CAPS.md` §7; **shipped dark**, `scope:'none'` on both themes — activation is a screenshot-loop aspect decision). Then item 6 **callouts DELIVERED AND MERGED** (`9f66930` — the author's first block-marking gesture: `Paragraph.callout` + `setCallout` + the Structure-station toggle; one chrome mechanism with the tint as the per-theme value, colours derived from the resolved accent, 0.96 CTO-locked on the real page; census: zero callout-shaped corpus content → the producer ships WITH the capability; lit-but-empty, parity stable by construction). Verified state on `main` post-merge (after `9f66930`): **backend 774/774, frontend 210/210, tsc + eslint clean both sides; `verify-real-export` 16/16, `verify-real-import` 4/4, `verify-real-publish` 4/4.** No new numbered ADR in that run (all mini-DRs and Level-2 reviews; the ADR-0051 closures are annexes/review records). All deliberately untagged, the tag decision still the CTO's; the S13 work is real progress against the **v0.14.0-alpha** row below (delivered + closed-on-evidence rather than a version-sized sprint); **Item 7 the Novel theme DELIVERED AND MERGED** (`1cc5a2b` — the third resident and the first to light drop caps; accent locked `#6E3B2F` on the real page; parity born with the ornament priced, §10.5; the subtitle-drop-cap limitation consigned with its `Chapter.subtitle` exit door). Then **Chapter.subtitle DELIVERED AND MERGED** (`be2b7a9`, `MINI_DR_SUBTITLE_FIELD` — the typed-but-dead field now lives end to end: the author's gesture, tri-format rendering priced in lock-step, ratio LOCKED 0.6 on the real-page taste stop; **Novel's consigned subtitle-drop-cap limitation retired with zero trigger code**; `SUBTITLE_IMPORT_MAPPING_UNBLOCK` A1-guarded). Post-subtitle verified state: **backend 795/795, frontend 213/213**, builds clean, harnesses green, corpus parity byte-locks unmoved. Then — same day — **the typography-quality cadrage** (`TYPOGRAPHY_QUALITY_SCOPE.md`, five measured constats, stopped at findings) **and the blockless-titles fix DELIVERED AND MERGED** (`1aeee04`, `MINI_DR_BLOCKLESS_TITLES`, 3 commits: a titled content with no blocks of its own is charged at every level — the cadrage-found live ADR-0051 drift that stranded an 18pt heading at the bottom of Modern's real p.40; `RenderMetrics.unplannedTitleBreaks` names the failure mode and §10.3's heading gate became a corpus test; parity consciously re-locked, every delta measured and attributed, Classic letter unmoved). Post-fix verified state: **backend 806/806, frontend 213/213**, builds clean, harnesses 4/4 · 16/16 · 4/4. **next: the founder traversal (the founder's manuscript through the pipeline, CTO-supplied DOCX), then the full DR TYPOGRAPHY_QUALITY (justification WITH real-dictionary hyphenation, one decision); LEADING_FIDELITY consigned after it; item 8 standing SKIP; item 9 waits for a natural lull.** The **v0.19.0-beta Editorial AI Engine** row is **suspended pending commercial revenue** (`NO_PAID_AI_BEFORE_REVENUE`), not merely deferred.

**Addendum (2026-07-23, `main` @ `bfb3ad7` + docs, all deliberately untagged):** the two FOUNDER-TRAVERSAL waves and the bidirectional structure chantiers landed. **The `<br>` boundary fidelity fix, class-wide** (`f6e38d2`, `BR_BOUNDARY_SCOPE` — the gravest text-corruption defect of the project: 215/185 `<br>` jamming the founder's sentences; one `collapseLineBreaks` helper at the single load seam, all 7 extraction sites; re-locked `art-of-captivating` 9280→9322, +42 = one word per `<br>`). Then **STRUCTURE_ASSIST** (`2c40d71` — the UNDER-structured pole: suggest chapters from markers the author typed as plain text; read-only detector + confirm-all; judge 14→1) and **STRUCTURE_CLEANUP** (`bfb3ad7` — the OVER-structured sibling: collapse the founder's 29 empty `CHAPTER n` / editorial markers into the real chapters that follow, on the SAME invariant; A1 remove + auto-number, A2 inherit-canonical-title-with-subtitle; the cadrage corrected the sibling DR's §9 `mergeChapterIntoPrevious` as measured-false-and-destructive; judge 29→1, near-blank pages 36→7 = the founder's image-5 offset repaired). Both bidirectional poles now shipped, both founder books the probe fixtures (`PRIVATE_MANUSCRIPT_FIXTURES` — not committed). Post-STRUCTURE_CLEANUP verified state: **backend 863/863, frontend 223/223**, builds + tsc + eslint(src) clean, live harnesses 4/4 · 16/16 · 4/4. No new numbered ADR (DRs + the §9 correction record). New standing precedent `SOLO_RENDER_VERIFICATION` (`TESTING_STRATEGY.md`).

**Addendum 2 (2026-07-23, `main` @ `a17e072`, still untagged):** FOUNDER TRAVERSAL 3 ran and CLOSED, then the AUTHOR_EXPERIENCE cadrage opened. Traversal 3 (the founder's third manuscript "Rachat et expiation bibliques 2", under-structured): **A1** `SUGGESTION_BUTTON_STATE` — the `EDITION_BUTTON_STATE` regression closed in both suggestion panels (Set of in-flight keys, only the in-flight button changes); **A2** `REPEATED_EDITORIAL_MARKERS` — a canonical editorial name at N>1 stops being proposed as a chapter (deductive: a book has one Conclusion; the suggester was FAITHFUL, the interpretation was the error); **A4** `EPUBRenderer` stops fabricating "Untitled" for an untitled preamble section (the "Unknown" class; the preamble is an ASTBuilder import artifact); **B5 `SUBCHAPTER_PROMOTION`** (`bcfa2ae` — the third structural form: a recurring editorial name becomes a section of each chapter; `promoteToSubsection` + the shared `repeatedEditorialKeys` D3 source + `SUBSECTION_APPLY_ORDER`; judge 26→1, chapters unchanged, drop caps kept). The structure suggester family is complete (assist/cleanup/subchapter, each silent when it has nothing to say). Verified: **backend 890/890, frontend 230/230**, harnesses 4/4 · 16/16 · 4/4. New: `CONTENT_DELETION_BY_AUTHOR` (an **ADR-0044 amendment** — author-facing deletion IN), the four founder design laws in `AUTHOR_EXPERIENCE.md`, `SUBSECTION_APPLY_ORDER` / `BATCH_CONFIRM_LATENCY` / `UNTITLED_PREAMBLE_NAMEABLE` / `DESCRIPTIVE_SUBHEADING_DETECTION` consigned. **Now IN FLIGHT: the `AUTHOR_EXPERIENCE` gap cadrage** (`AUTHOR_EXPERIENCE_GAP.md`) — seven axes measured, awaiting CTO verdicts → Design Review → mockups → founder validation → build (no code before that). Load-bearing finding: criterion A (fluidity) needs incremental rendering + `BATCH_CONFIRM_LATENCY` as prerequisites. **Next: the AUTHOR_EXPERIENCE DR, then `TYPOGRAPHY_QUALITY`.**

**Addendum 3 (2026-07-24, MERGED to `main` — `--no-ff` merge `d6e0cd5`; `feat/incremental-render` deleted):** INCREMENTAL_RENDER P1 (AUTHOR_EXPERIENCE Axis-7 prerequisite, after P2 `BATCH_CONFIRM_LATENCY`) is **CLOSED and MERGED; founder taste-stop CLOSED (surface validated → one fit-width regression found by the founder → fixed on-branch → re-validated). Post-merge gate on `main`: backend 921/921, frontend 238/238, tsc + eslint + builds clean, verify-real-* 4/4·16/16·4/4 (throwaway server, zero trace), real store at the 9-project baseline. AUTHOR_EXPERIENCE Design Review opens on the CTO's word.** Six commits: `renderPageRange` grown to draw a visible page range from the full `PaginatedBook`'s own `Page` objects — mid-content, chapter/section openings (drop cap + title chrome), and the split-tail seam (reusing the one cut implementation, both boundaries) — so **page-region ≡ page-export by construction**; a `PageRangeRenderer` port + `GET /api/projects/:id/region`; pdfjs-dist@6.1.200 locked; a PDF.js canvas Proof surface replacing the `<embed>` (standard selectable text layer — a11y gate; scroll preserved across a re-ink); window policy (visible ±1 eager + render-on-scroll) + the 500 ms debounce consigned revisable. **Judge (on the founder's EDITED copy of book 3 `…d7bticjiw` v34/32ch, a5/classic/comfort, 445 pages — re-attributed from the original `…cy7m12l0w` v22 the probes first used): engine 155 ms ≤ 300 ms hot (region render 31 ms; full 650 ms → 4.2×); the three-page fidelity invariant green; scroll preserved (jsdom + real-Chromium Playwright); backend 921/921, frontend 234/234, tsc + eslint + builds clean; verify-real-* 4/4·16/16·4/4 on a throwaway server, zero store trace. Plus the `STORE_DEFAULT_SAFE` guard (createApp defaults to :memory:; real store via explicit index.ts opt-in) closing the ad-hoc-store class behind the P1 `region`-trace incident.** No new numbered ADR (the design lives in `INCREMENTAL_RENDER_DR.md`; C2 "fixed floor" retraction and the candidate-1/D-decisions are recorded there and in `INCREMENTAL_RENDER_SCOPE.md`). Candidate 3 (partial repagination) stays consigned second-order — the engine is under budget hot. The frontend's live region-fetch edit-loop wiring is a stated boundary → AUTHOR_EXPERIENCE. **Next: founder taste-stop → merge → the AUTHOR_EXPERIENCE Design Review.**

**Addendum 4 (2026-07-24, `main` @ `40c3b5d`, no code — DR + plan only):** the **AUTHOR_EXPERIENCE Design Review is OPEN, GATED, and FOUNDER-VALIDATED** (`AUTHOR_EXPERIENCE_DR.md`, the project's largest chantier). The CTO gated the full decision set (D1 Option B — a projected `EditorialSkeleton` read-model with a single-write-path invariant + a projection-coherence test; D2 dedication+preface; D3 contextual editing; D4 the live region-fetch loop wiring P1's engine; D5 real-page theme thumbnails + separators/opening into the theme; D6 the document title surface; D7 the disappearances; D8 impossible-by-gentleness). The six mockups were produced (`mockups/author-experience-mockups.html`, real corpus content, both themes) and the **founder validated all six**, ruling the two delegated decisions (provisional-total costume = "≈", consigned-revisable; **`Overview` removed** — the studio opens directly into the workspace, its facts re-homed, §4 table). The **§8 CONSTRUCTION PLAN is produced — awaiting the CTO's gate**: 4 milestones (M0 D1 foundation · M1 workspace shell reads · M2 editing + the live region loop · M3 compose + theme/title surfaces, founder taste-stops · M4 dissolve the old stations + felt-A live stop), ~15 gated commits, **each milestone a working studio**, branch at the gate. No new numbered ADR (DR-level decisions; the `STORE_DEFAULT_SAFE` guard and the shell-cleanup registry rule are recorded in `TODO.md`/`DEVELOPMENT_WORKFLOW.md`). **Next: the CTO gates §8 → branch → build M0…M4.**

**Addendum 5 (2026-07-24, session-close — all on branches, nothing merged, still untagged):** the §8 plan was **gated** and **M0–M2 BUILT on `feat/author-experience`** (@ `80595b0`): M0 the `projectEditorialSkeleton` D1 read-model + two locked tests; M1 the read studio (skeleton · document · permanent Proof); M2 contextual editing (D3) + the live region-fetch loop (D4, "≈" provisional total). The M2 engine-timing gate was **settled by attribution** — the felt tax was the O(v) `findById`, not the render — opening a **bounded persistence interlude before M3**: **`APPEND_ONLY_PERSISTENCE` (option B) DR gated**, **step 1 built on `feat/append-only-persistence`** (@ `ff70910`, the additive `getVersion`/`appendVersion` port), self-stopped before the `findById` O(1) flip + the founder's 93 MB store migration (fresh-session first move = the dry-run on a copy, DR §9). The product display-name **`Unveil`** is recorded (`AUTHOR_EXPERIENCE.md` §PN), shipping at M4, trademark-gated. **No new numbered ADR** (DR-level; the ADR-0048 amendment lands when B's flip ships). `main` untouched at the §8-gate docs commit; **the tag decision remains the CTO's.**

**Addendum 6 (2026-07-25, MERGED into `feat/author-experience` @ `3da856d`, still untagged; `main` untouched):** the persistence interlude BUILT, JUDGED GREEN, and merged back into the arc (`--no-ff`, post-merge gate green — backend 945/945, frontend 248/248, tsc+eslint+builds clean, harnesses 4/4·16/16·4/4 throwaway zero trace). **Standing rule:** the founder store is schema v2 and `main`'s code is v1-era, so studio activation happens ONLY from `feat/author-experience` until the arc merges (the flip is entangled with M0–M2 in the branch history — no persistence-only merge to `main`). `APPEND_ONLY_PERSISTENCE` option B (the **ADR-0048 amendment** — DECISIONS.md): the whole-aggregate read/write becomes append-only — `findById` returns the head + a version INDEX from metadata columns (no payload), `getVersion` loads one on demand, `save` is head-only, version creation flows through the append-only atomic idempotent `appendVersion`. Step 2 the flip (`5acd6b3`, backend 945/945); Move 3 the real-store migration to **schema v2** (backup byte-verified first, `user_version` 1→2, data hash unchanged vs a pristine v1 copy, book 3's `v32 "publication"` backfilled `milestone=1`, 9 head books whole); Move 4 the judge — the **O(v) read tax is gone: findById 58 ms @ v34, down from 1736 ms** (the residual tracks head-book size, not version count), full gesture ~229 ms sub-second, undo byte-identical, harnesses import 4/4·export 16/16·publish 4/4 on a throwaway server (zero trace). Milestone MODEL support ships (automatic `{'publication'}` — export creates no version, `EXPORT_MILESTONE_VERSION` consigned); **D (version-log cap) stays deferred** — the felt O(v) tax that argued for pulling it forward is eliminated. **No new numbered ADR** (the ADR-0048 amendment is recorded in place). `main` untouched; the tag decision remains the CTO's. **Next: the CTO's merge word → AUTHOR_EXPERIENCE M3 resumes (D2 dedication+preface, D5 theme thumbnails/separators, D6 title surface — founder taste-stops).**

| Version | Milestone | Status | Tag | Notes |
|---|---|---|---|---|
| v0.1.0-alpha.1 | Prototype — Domain + Infrastructure | ✅ Released | `v0.1.0-alpha.1` | Book AST model, ASTBuilder, HtmlNormalizer. See `docs/releases/` (none written for this tag retroactively — first Release Notes doc starts at v0.2.0-alpha). |
| v0.2.0-alpha | Import Pipeline | ✅ Released | `v0.2.0-alpha` | Full Application + Presentation layers, `POST /api/manuscripts/import`, 88 tests, CI/lint/coverage tooling. See `docs/releases/v0.2.0-alpha/ReleaseNotes.md`. |
| v0.3.0-alpha | Rendering Engine | ✅ Released | `v0.3.0-alpha` | Theme Engine, Layout Engine, `Renderer` port, DOCX export (PR #1, merge commit `32ac220`). 118 tests passing (up from 88). See `docs/releases/v0.3.0-alpha/ReleaseNotes.md`. |
| v0.4.0-alpha | PDF Export | ✅ Released | `v0.4.0-alpha` | `PDFRenderer` (PDFKit, ADR-0014), `POST /api/manuscripts/export` gains a `format=pdf` field (PR #3, merge commit `820f1ef`). 125 tests passing (up from 118). See `docs/releases/v0.4.0-alpha/ReleaseNotes.md`. Originally scoped as "Professional Export" covering both PDF and EPUB together; split in two after PDF landed first per the CTO-directed Sprint 3A/3B re-sequencing — EPUB is now its own row below. |
| v0.4.1-alpha | EPUB Export | ✅ Released | `v0.4.1-alpha` | `EPUBRenderer` (`epub-gen-memory`, ADR-0020), `POST /api/manuscripts/export` gains a `format=epub` field (PR #4, merge commit `a7a38a0`). 133 tests passing (up from 125). See `docs/releases/v0.4.1-alpha/ReleaseNotes.md`. Resolves ADR-0015 (spike + library decision recorded in ADR-0020). Completes the "Professional Export" milestone originally planned as one v0.4.0-alpha release, split into v0.4.0-alpha (PDF) and this row (EPUB) per the CTO-directed Sprint 3A/3B re-sequencing. Tag cut 2026-07-17 per ADR-0021 (post-Sprint-3 governance pass). |
| v0.5.0-alpha | Typography Engine | ✅ Released | `v0.5.0-alpha` | `TypographyResolver` pipeline (inline formatting, drop caps, English-only smart quotes, widow/orphan avoidance), real PDF font embedding (Gelasio/Inter/JetBrains Mono), `QualityMetrics` fully activated (PR #9, merge commit `27a4347`). 195 tests passing (up from 133). See `docs/releases/v0.5.0-alpha/ReleaseNotes.md` and `docs/releases/v0.5.0-alpha/SPRINT_4_FINAL_REPORT.md`. Originally scoped to also include a fuller `ValidatorEngine` (readability/completeness scoring) alongside Typography — split out during the sprint's own Design Review; `ValidatorEngine` is now a separate, not-yet-scheduled future milestone (see `docs/TODO.md`'s CTO priority order, item 2). |
| v0.6.0-alpha | Validation Engine | ✅ Released | `v0.6.0-alpha` | `ValidationEngine` orchestrating a `RuleRegistry` of 8 independent rules (structural, metadata, heading hierarchy, typography, images, hyperlinks, KDP/EPUB pre-render readiness), `QualityScore` composite scoring, wired into `ImportManuscriptUseCase` (PR #10, merge commit `3032d70`). 282 tests passing (up from 195). See `docs/releases/v0.6.0-alpha/ReleaseNotes.md` and `docs/releases/v0.6.0-alpha/SPRINT_5_FINAL_REPORT.md`. **This row's milestone was corrected from an earlier, superseded assumption:** it previously read "Premium UI/UX" (guessed before Sprint 5's actual scope was decided by its own Design Review, `docs/architecture/diagrams/PLATFORM_ARCHITECTURE_ROADMAP.md`) — that row's content moved to v0.7.0-alpha below, and every subsequent row shifted down one version accordingly, since none of them had been released under their old number. |
| v0.7.0-alpha | Professional Layout Engine | ✅ Released | `v0.7.0-alpha` | `LayoutEngine` extended (not replaced, ADR-0029) with real `PageLayout` presets (A4/A5/KDP 5x8/5.5x8.5/6x9, ADR-0030), `LayoutSelector` port + `ManualLayoutSelector` (replaces hardcoded `LetterPageLayout`), `Theme.runningHead` (`RunningHead`, `ClassicTheme` populated), real per-page header/footer in `PDFRenderer`/`DOCXRenderer` (drops the hardcoded `'Book Publisher Studio'` string), `Chapter.openingPageStyle`/`startPageNumber` honored, automatic Table of Contents generation (PR #11, merge commit `eb05beb`). 328 tests passing (up from 282). Two real bugs found and fixed during real-file verification, ADR-0031: neither renderer had ever consumed `PageLayout` at all (only `LetterPageLayout` existed before this sprint, so the gap was invisible); TOC generation originally walked only `Heading` blocks, which real DOCX imports never produce (real headings become `Chapter`/`Section` titles instead). See `docs/releases/v0.7.0-alpha/ReleaseNotes.md` and `docs/releases/v0.7.0-alpha/SPRINT_6_FINAL_REPORT.md`. **This row's milestone was corrected from an earlier, superseded placeholder:** it previously read "Premium UI/UX" (guessed before Sprint 6's actual scope was decided by its own Design Review) — that row's content moved to v0.8.0-alpha below, and every subsequent row shifted down one version accordingly, since none of them had been released under their old number (same pattern as the v0.6.0-alpha renumbering one row above). |
| v0.8.0-alpha | First Demonstrable Product | ✅ Released | `v0.8.0-alpha` | A real Next.js frontend (`packages/shared-types` npm workspace, ADR-0033) drives the real stateless backend end to end: real DOCX import, real book structure, real validation findings, real layout/theme selection, a real on-demand PDF preview, real PDF/DOCX/EPUB downloads — 8 new frontend components, no mocks anywhere (PR #12, merge commit `f17fd65`). Backend tests unchanged at 336 (no backend logic changed after Commit 3's DTO migration); frontend has no automated test suite yet, disclosed not hidden. See `docs/releases/v0.8.0-alpha/ReleaseNotes.md` and `docs/releases/v0.8.0-alpha/SPRINT_7_FINAL_REPORT.md`. **This row's milestone was corrected from an earlier, superseded placeholder:** it previously read "Premium UI/UX" (guessed before Sprint 7's actual scope was decided by its own Design Review, which renamed the sprint to "First Demonstrable Product") — same renumbering pattern as the v0.6.0-alpha/v0.7.0-alpha corrections above, except this time the row's own version number didn't shift (Sprint 7 was always going to be v0.8.0-alpha; only the milestone name and scope description were placeholders). |
| v0.9.0-alpha | Publishing Engine | ✅ Released | `v0.9.0-alpha` | The first component in this project's history sitting **downstream of `Renderer`**: a `PublishingTarget` port (ADR-0012 shape) with `KDPTarget` as its only implementation, `Packaging`/`PublishingBundle`, `SubmissionValidator` orchestrating 4 `PostRenderValidationRule`s behind a `ValidationRuleProvider` port (ADR-0036), `PublishingUseCase`, and `POST /api/manuscripts/publish` returning a real itemized `PublishingReport` (PR #13, merge commit `4a4deaa`). 386 tests passing (up from 336), 93.41% statement coverage. **Amazon KDP only, validation and packaging only — no account, no Amazon API call, no credentials, no publication event** (Design Review Decision 5, held without exception). Requirements verified by a real spike against Amazon's own published documentation before any code (ADR-0035), which caught a wrong interface shape: the paperback cover has *computed* dimensions, not the fixed pixel size the eBook cover uses. Two standing governance rules locked — ADR-0036 (platform rules behind a `RuleProvider` port; no `if (platform === 'kdp')` anywhere in the engine) and ADR-0037 (engine objects are platform-agnostic; platforms depend on the engine, never the inverse). **ADR-0038 left OPEN, deliberately:** real pagination metrics never reach `PageCountRule` — found by real-fixture verification, invisible to 386 passing tests, deferred because closing it is a contract evolution warranting its own Design Review. See `docs/releases/v0.9.0-alpha/ReleaseNotes.md` and `SPRINT_8_FINAL_REPORT.md`. **This row's milestone was corrected from an earlier, superseded placeholder:** it previously read "Plugin System" (assigned before Sprint 8's own Design Review chose Publishing Engine) — Plugin System shifts one row later, the same renumbering pattern already applied at v0.6.0-alpha, v0.7.0-alpha, and v0.8.0-alpha. |
| v0.10.0-alpha | UI Foundation | 🔀 Content merged to `main` (PR #14), untagged | — | **Sprint 9.** Design System, reusable components, themes, typography, colours, icons, grid, responsive, accessibility, navigation. **No new business logic** — the backend is untouched. First milestone of the reordered roadmap (ADR-0039). |
| v0.11.0-alpha | UX & Workflow | 🔀 Largely absorbed by HOME_WORKSPACE/PRODUCT_EXPERIENCE (merged, PR #14) | — | **Sprint 10.** The real editorial journey end to end (home → new manuscript → import → validation → correction → layout → preview → export → publication), click count, comprehensibility, error messages, guidance. Deliberately a separate sprint from UI (ADR-0039): UI answers "what does it look like?", UX answers "is it pleasant and efficient to use?". Frontend-only; still compatible with the stateless backend. |
| v0.12.0-alpha | Workspace & Project Management | 🔀 Content merged to `main` (PR #14: SQLite persistence ADR-0048, library, workspace), untagged | — | **Sprint 11.** Projects, recent manuscripts, favourites, history, search, organisation — moving the user from working on *a file* to working on *a library*. **Requires a persistence layer that does not exist and is currently forbidden** by Sprint 7 Decision 2 (stateless backend); amending that decision is a prerequisite and needs its own Design Review (ADR-0039). |
| v0.13.0-alpha | Autosave & Recovery | ⏳ Planned | — | **Sprint 12.** Autosave, crash recovery, local versions, restoration — guaranteeing no work is lost *before* there are many users. Depends on Sprint 11 persistence. |
| v0.14.0-alpha | Performance & Scalability | ⏳ Planned | — | **Sprint 13.** Large DOCX/PDF, memory, speed, pagination, rendering. Where real problems usually surface. Note the real current baseline: the largest manuscript ever processed here is 39,913 words / 134 estimated pages, imported in 253ms and exported to PDF in 598ms (Sprint 7 Commit 10). |
| v0.15.0-beta | Collaboration | ⏳ Planned | — | **Sprint 14.** Comments, sharing, review, history, version comparison. Depends on Sprint 11 persistence. |
| v0.16.0-beta | Cloud Sync | ⏳ Planned | — | **Sprint 15.** Synchronisation, cloud backup, multi-device — only after the local version is stable. Depends on Sprint 11 persistence. |
| v0.17.0-beta | Licensing | ⏳ Planned | — | **Sprint 16.** Activation, licences, subscriptions, Community and Pro editions. Supersedes the older "Licensing & Cloud" row, which bundled this with Sprint 15. Depends on Sprint 11 persistence. |
| v0.18.0-beta | Telemetry & Observability | ⏳ Planned | — | **Sprint 17.** Non-personal indicators only — average import time, error frequency, most-used features, performance — to direct future work with evidence rather than assumption. Depends on Sprint 11 persistence. |
| v0.19.0-beta | Editorial AI Engine | 🛑 Suspended pending commercial revenue | — | **Sprint 18+ — SUSPENDED (not merely deferred) until the first commercial revenue-generating phase** (founder directive 2026-07-21; unblock = real revenue, not a date, not a sprint estimate — single source of truth: `docs/TODO.md` → `NO_PAID_AI_BEFORE_REVENUE`, ADR-0039 annex in `docs/DECISIONS.md`). Originally moved here by ADR-0039 (high value, but not indispensable for a usable product, better designed from observed needs than assumed ones); that strategic deferral is now **hardened** into a cost-gated suspension. The round-1 Design Review is preserved rather than withdrawn — 6 open questions, real code evidence, one unresolved accept/reject-vs-stateless tension. |
| v0.20.0-beta | Plugin System | ⏳ Planned, strategically deferred | — | **Sprint 19+.** Moved here from  by ADR-0039: only valuable once several AI engines or extensions actually need integrating. Sandboxed, versioned plugins operating on the Book AST, plus the AI-provider abstraction ( §2.3). |
| v1.0.0 | Commercial Release | ⏳ Planned | — | Full licensing enforcement, payment processing, plugin/theme marketplace. |

## How this maps to `docs/VISION.md`'s stage progression

```
MVP (v0.1.0 - v0.8.0-alpha) → Beta (v0.9.0-alpha - v0.20.0-beta) → Public Beta → Commercial Launch (v1.0.0) → Enterprise
```

(Boundary shifted with the 2026-07-17 renumbering above, same day as the row above it — same content boundary, Premium UI/UX still the last MVP milestone, just under its corrected version number. **Beta's upper bound moved from `v0.11.0-beta` to `v0.12.0-beta` on 2026-07-18**, when Sprint 8 took the `v0.9.0-alpha` slot for Publishing Engine and every later row shifted one version — the MVP boundary itself is unchanged, and `v0.9.0-alpha` is now the first *released* Beta-stage version. **Moved again the same day, to `v0.20.0-beta`, by ADR-0039's strategic reprioritization** — Sprints 9–17 now target product completeness (UI, UX, Workspace, Autosave, Performance, Collaboration, Cloud Sync, Licensing, Telemetry) ahead of Editorial AI Engine and Plugin System, which move to `v0.19.0-beta`/`v0.20.0-beta`. The Beta stage is longer, not different: the MVP boundary and the v1.0.0 target are both unchanged.)

## Updating this file

- Add a row when a new version is planned, even before it's built — mirrors how ADR-0012–0016 were written before Sprint 2 code
- Move a row's Status to ✅ Released only after the tag is actually pushed (matches the discipline established for `v0.2.0-alpha`: tag, then Release Notes, then this index)
- Never delete a row — like `docs/DECISIONS.md`, this is meant to read as history in a few years, not just current status
